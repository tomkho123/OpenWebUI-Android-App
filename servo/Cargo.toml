[package]
name = "openwebui-servo"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
log = "0.4"

# Servo dependencies - will add actual Servo crates later
# For now, using placeholder dependencies
libc = "0.2"

[features]
default = []
